export class TotalInfo {
  constructor(totalProducts, totalSchools, totalStudents, totalPacks) {
    this.totalProducts = totalProducts;
    this.totalSchools = totalSchools;
    this.totalStudents = totalStudents;
    this.totalPacks = totalPacks;
  }
}
